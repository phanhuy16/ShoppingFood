
<!-- Navbar start -->
<div class="container-fluid fixed-top">
	<div class="container topbar bg-primary d-none d-lg-block">
		<vc:header-top></vc:header-top>
	</div>
	<div class="container px-0">
		<nav class="navbar navbar-light bg-white navbar-expand-xl">
			<a asp-action="Index" asp-controller="Home" class="navbar-brand"><h1 class="text-primary display-6">Fruitables</h1></a>
			<button class="navbar-toggler py-2 px-3" type="button" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
				<span class="fa fa-bars text-primary"></span>
			</button>
			<div class="collapse navbar-collapse bg-white" id="navbarCollapse">
				@await Html.PartialAsync("_Menu")
				<div class="d-flex align-items-center m-3 me-0">
					<button class="btn-search btn border border-secondary btn-md-square rounded-circle bg-white me-3" data-bs-toggle="modal" data-bs-target="#searchModal"><i class="fas fa-search text-primary"></i></button>
					<a asp-action="Index" asp-controller="Cart" class="position-relative me-1 my-auto">
						<i class="fa fa-shopping-bag fa-2x"></i>
						<span class="position-absolute bg-secondary rounded-circle d-flex align-items-center justify-content-center text-dark px-1" style="top: -5px; left: 15px; height: 20px; min-width: 20px;" id="checkout-items"> 0 </span>
					</a>
					@if (User.Identity.IsAuthenticated)
					{
						<div class="nav-item dropdown">
							<a asp-action="Profile" asp-controller="Account" class="my-auto nav-link">
								<i class="fas fa-user-check fa-2x"></i>
							</a>
							<div class="dropdown-menu m-0 bg-secondary rounded-0">
								<span class="dropdown-item"> Hi, @User.Identity.Name </span>
								<a asp-action="Logout" asp-controller="Account" class="dropdown-item"> <i class="fas fa-sign-out-alt"></i> Logout </a>
							</div>
						</div>
					}
					else
					{
						<div class="nav-item dropdown">
							<a asp-action="Login" asp-controller="Account" class="my-auto nav-link">
								<i class="fas fa-user fa-2x"></i>
							</a>
							<div class="dropdown-menu m-0 bg-secondary rounded-0">
								<a asp-action="Login" asp-controller="Account" class="dropdown-item"> <i class="fas fa-sign-in-alt"></i> Sign in </a>
								<a asp-action="Register" asp-controller="Account" class="dropdown-item"> <i class="fas fa-user-plus"></i> Sign up </a>
							</div>
						</div>
					}
				</div>
			</div>
		</nav>
	</div>
</div>
<!-- Navbar End -->